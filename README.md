# 🧩 Webpack Template (No Babel)

This repository is a clean and minimal **Webpack 5 template** for starting JavaScript projects without using Babel.

It includes:

- Simple project structure  
- Separate configs for **development** and **production**  
- **HtmlWebpackPlugin** for automatic HTML generation  
- **html-loader** to import HTML  
- **css-loader** + **style-loader** for styling  
- Asset support (images, etc.)  
- No Babel – using native JavaScript

---

## 🚀 Getting Started

### 1. Create a New Repo From This Template

If this repo is marked as a GitHub Template:

1. Click **"Use this template"**  
2. Choose a name for your new repo  
3. Clone it:

```bash
git clone <your-new-repo-url>
cd <your-new-repo>
npm install
npm run dev
npm run build
‍‍‍‍```
